docker rmi authentication:latest -f && docker build -t authentication:latest . && dk images

docker tag authentication:latest  phucthaihg/authentication:1.0

docker image push phucthaihg/authentication:1.0
